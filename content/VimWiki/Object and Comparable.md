# `Object` and `Comparable` in C++
#cpp 

##### `Object`:
- have zero-parameter constructor
- `operator=`
- copy constructor
##### `Comparable`:
- everything of `Object`
- `operator<`
We can use [[operator overloading]] to define our own operator.
